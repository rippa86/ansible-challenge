---
- name: Hello World Ansible Island Challenge Treasurehunt
  gather_facts: true
  hosts: all
  tasks:

    - name: Display facts
      ansible.builtin.debug:
        var: ansible_facts

    - name: Display facts
      ansible.builtin.set_stats:
        data:
          name: "{{ ansible_facts.ansible_machine }}"